#include <iostream>
#include <vector>
#include <map>
#include <set>
#include <string>
using namespace std;

void DoSomething(const map<int, int>& m);

int main(){
    map<string, set<string>> Datos;
    string year, month, day, evento, fecha, com

}

void DoSomething(const map<int, int>& m) {
if (m.count(key) > 0) {
    value = m.at(key);
    }
}
